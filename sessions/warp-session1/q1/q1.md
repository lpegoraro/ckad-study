I am preparing for CKAD exams, can you provide some kubernetes examples of questions and evaluate if my input and process are valid for this?
I'd be happy to help you prepare for the CKAD (Certified Kubernetes Application Developer) exam! I can provide you with example questions and evaluate your responses and approach.

Here's how we can work together:

1. I'll give you CKAD-style questions covering topics like:
◦  Pod creation and management
◦  Deployments, StatefulSets, and DaemonSets
◦  Services and networking
◦  ConfigMaps and Secrets
◦  Volumes and storage
◦  Resource limits and requests
◦  Debugging and monitoring
◦  Multi-container pods
◦  Init containers and sidecar patterns
2. You attempt the question by:
◦  Writing YAML manifests
◦  Providing kubectl commands
◦  Explaining your approach
3. I'll evaluate your answer by checking:
◦  Correctness of the YAML syntax
◦  Whether it solves the stated problem
◦  Best practices and efficiency
◦  Kubernetes concepts understanding
◦  Command accuracy and flags

Let's start! Here's your first question:



Question 1: Create a Pod with Resource Limits

Create a Pod named resource-pod in the default namespace with:
•  Container image: nginx:1.21
•  CPU request: 100m, CPU limit: 200m
•  Memory request: 128Mi, Memory limit: 256Mi
•  The pod should restart only if it crashes (not on other failures)

Please provide:
1. The YAML manifest for this Pod
2. The kubectl command to apply it
3. A command to verify the resource limits were applied correctly


Go ahead and attempt this question, and I'll evaluate your response!


> answering the first question, the YAML file is resource-pod.yaml, the command to apply is `k apply -f ./resource-pod.yaml` and the command to verify I am having some issues on executing, I would say `k describe resource-pod` and do a grep or a cut to get the limits
Good approach! Let me evaluate your answer and provide feedback:

Evaluation

✅ Correct:
•  Using kubectl apply -f ./resource-pod.yaml is correct
•  Using kubectl describe to verify is the right approach
•  Recognizing you need to filter the output shows good thinking

📝 Improvements:

1. kubectl alias: You're using k which is great for the exam (time-saver!)
2. Describe command syntax: Should be:
bash
   (Need to specify the resource type pod)

3. Better verification commands:
bash
Now, let me check if you actually created the YAML file:
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/resource-pod.yaml
YAML Review

❌ Issues found:

1. Resource units missing (lines 13-17):
yaml
2. RestartPolicy location (line 18):
◦  restartPolicy is at the pod level (spec.restartPolicy), not container level
◦  Move it up one level in the YAML hierarchy
◦  For "restart only if crashes", use restartPolicy: OnFailure or Always (default is Always)
◦  Note: OnCrash isn't a valid value!
3. Extra field (line 19):
◦  Remove status: {} - this is managed by Kubernetes

✅ Correct YAML:
yaml
Key Takeaways for CKAD:
•  Always include units for CPU (m for millicores) and memory (Mi, Gi, etc.)
•  Understand YAML hierarchy - restartPolicy is pod-level, not container-level
•  Valid restartPolicy values: Always (default), OnFailure, Never