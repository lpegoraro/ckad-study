Question 4
Question 4: Deployment with Rolling Update Strategy

Create a Deployment and manage its updates.

Part A - Create Deployment:
1. Create a Deployment named web-app in the default namespace with:
◦  Image: nginx:1.19
◦  Replicas: 3
◦  Labels: app=web, tier=frontend
◦  Rolling update strategy with:
▪  Max surge: 1
▪  Max unavailable: 1
◦  Add a liveness probe that checks HTTP GET on path / port 80
◦  Add a readiness probe that checks HTTP GET on path / port 80 with initial delay of 5 seconds

Part B - Update and Rollback:
2. Update the deployment to use image nginx:1.20 and record the change
3. Check the rollout status
4. View the rollout history
5. Rollback to the previous version

Please provide:
1. YAML manifest for the Deployment (or imperative command to create it)
2. Command to update the image to nginx:1.20 with recording enabled
3. Command to check rollout status
4. Command to view rollout history
5. Command to rollback to the previous version
6. Command to verify the rollback was successful (check the image version)



CKAD Tip: Deployments and rollout management are heavily tested. Know imperative commands for speed!

Go ahead and attempt this question!
~/workspace/study/ckad-prep/warp-session1
Part A
Firstly I created the deployment using this command 
`k create deployment web-app -n default --image=nginx:1.19 --replicas=3 -o yaml > web-app-deployment.yaml`
creating a YAML so I can edit the parameters that are better in a Editor, then I added the labels, editted the rolling update strategy and added the probes, in the file `web-app-deployment.yaml`

Part B
I could set the image by using the imperative command
`k set image deployment/web-app web-app=nginx:1.20`,  but for deployments I prefer YAML files to be the source of truth, so I removed the metadata from the generated yaml, mainly the immutable fields like the uid, and updated the image to 1.20 and did a `k apply -f web-app-deployment.yaml`, I also checked differences between the deployment and the current in using the `k get deployment web-app -o yaml > web-app-current.ymal` and `k diff -f web-app-deployment.yaml -f web-app-current.yaml` 

For rolling out, I followed up the `k rollout status deployment/web-app` and did the rollback by imperative command since it would be done imperatively in a crisis situation in a production environment by running the command `k rollout undo deployment/web-app`
I think I missed the recording enabled piece
Excellent work! Your approach shows strong understanding and good production habits. Let me evaluate:

Evaluation

✅ Great Practices:
•  Using k create deployment ... -o yaml > file.yaml is perfect for the exam - fast imperative generation, then edit
•  Your philosophy about YAML as source of truth is production-grade thinking
•  k rollout status and k rollout undo commands are correct
•  Removing immutable fields before re-applying is good awareness

📝 Feedback on Part B:

The k diff command issue:
bash
kubectl diff only takes ONE file and compares it against the cluster state:
bash
For comparing two files, use regular diff:
bash
Recording changes:
You correctly identified this was missing! Two ways to record:
bash
Note: --record is deprecated but still works and may appear in CKAD. It adds the command to the rollout history annotations.

To view rollout history:
bash
Now let me check your YAML:
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/web-app-deployment.yaml

1. Line 4 - generation field:
yaml
This is a system-managed field. You should remove it along with other immutable/status fields.

2. Line 48 - status field:
yaml
You mentioned removing immutable fields, but status is still there.

3. Lines 6-7 - Label location:
yaml
The question asked for labels app=web, tier=frontend. You have:
•  ❌ Deployment metadata has app: web-app (should be app: web)
•  ❌ Pod template (line 25) only has app: web-app, missing tier: frontend

✅ Correct structure:
yaml
4. Line 16 - Selector mismatch:
Since you changed the labels, the selector needs to match. Currently it's app: web-app but should align with your label strategy.

✅ Everything else looks great:
•  Rolling update strategy is perfect (maxSurge: 1, maxUnavailable: 1)
•  Liveness probe configured correctly
•  Readiness probe with initialDelaySeconds: 5 is correct
•  Image updated to nginx:1.20
•  Replicas: 3

Key Takeaways for CKAD:
•  Always remove these fields from generated YAML before applying: generation, resourceVersion, uid, creationTimestamp, status
•  Labels must be consistent: Deployment metadata → selector → pod template
•  kubectl diff -f file.yaml compares file vs cluster (not file vs file)
•  --record flag stores command in annotations for rollout history (deprecated but still in exam)
•  Use k rollout history deployment/name to see revision history

Score: 85/100 - Solid approach and understanding! Just watch label consistency and cleaning generated YAML.

Ready for question 5?