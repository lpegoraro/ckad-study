Question 5: Services and Network Policies

You need to set up networking for a microservices application.

Part A - Create Services:
1. Create a Deployment named backend with:
◦  Image: nginx:alpine
◦  Replicas: 2
◦  Labels: app=backend, tier=api
◦  Container listens on port 80
2. Create a ClusterIP Service named backend-svc that:
◦  Exposes the backend deployment on port 8080
◦  Targets container port 80
◦  Uses the appropriate selector
3. Create a Deployment named frontend with:
◦  Image: nginx:alpine
◦  Replicas: 1
◦  Labels: app=frontend, tier=web
4. Create a NodePort Service named frontend-svc that:
◦  Exposes the frontend deployment on port 80
◦  Uses NodePort 30080
◦  Uses the appropriate selector

Part B - Testing:
5. Test connectivity from a frontend pod to the backend-svc service using curl
6. Verify the NodePort service is accessible

Please provide:
1. Commands or YAML to create both deployments
2. Commands or YAML to create both services
3. Command to test connectivity from frontend to backend service
4. Command to verify the NodePort service endpoint
5. Command to list all services and show their types and endpoints



CKAD Tip: Know the imperative commands for creating services - they're much faster than writing YAML! Use kubectl create service and kubectl expose.

Go ahead and attempt this question!

Answer
Firstly I created the YAML using the following command.
`k create deployment backend --image=nginx:alpine --replicas=2 --dry-run=client -o yaml > backend-deployment.yaml`
Then I edited the YAML to add the Labels and containerPort
Remembering to add labels in all three places: metadata, selector and template

Then I created the YAML for ClusterIP Service, also following the same standard
`k create service clusterip backend-svc --tcp=8080:80 --dry-run=client -o yaml > backend-svc-clusterip.yaml`
In this I had to just adjust the selector to match the app: backend and tier: api, to properly select the backend deployment.

For the frontend I did the the creation directly with imperative, then I edited the YAML, I need to remember more to add dry run in the call, I did the output -yaml, but I had to remove the metadata and status and delete it and re-apply

But I got it working and then I created the nodeport service using the command 
`k create service nodeport frontend-svc --tcp=80:80 --node-port=30080 --dry-run=client -o yaml >> frontend-svc-nodeport.yaml` and then I edited the labels and changed the name to be frontend-svc.

Now the cURL part I got a bit lost, could you explain the reason for it?

Excellent work! Your process is getting much cleaner. Let me address the curl question first, then evaluate your work.

Why Test with curl?

In CKAD, you need to verify your work actually functions! Creating resources isn't enough - you must prove they work correctly.

The curl test verifies:
1. ✅ The Service DNS resolution works (backend-svc.default.svc.cluster.local or just backend-svc)
2. ✅ The Service selector correctly targets the backend pods
3. ✅ Network connectivity exists between frontend and backend
4. ✅ The port mapping (8080 → 80) works correctly

How to do it:
bash
What you should see:
•  HTML response from nginx (the backend)
•  This proves the service networking works!

For NodePort verification:
bash
Now let me check your YAML files:
Finding files that match the following patterns in /home/lpegoraro/workspace/study/ckad-prep/warp-session1
 - *backend*.yaml
 - *frontend*.yaml
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/backend-deployment.yaml
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/backend-svc-clusterip.yaml
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/frontend-deployment.yaml
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/frontend-svc-nodeport.yaml
YAML Review

✅ backend-deployment.yaml - PERFECT!
All labels in the right places (metadata, selector, template). ContainerPort specified. Well done!

⚠️ backend-svc-clusterip.yaml

Lines 17-18:
yaml
Status fields should be removed when using --dry-run=client. This might have been left from a previous attempt.

Lines 4-5:
yaml
Service labels don't have to match the pods. This is fine, but for consistency, you could use app: backend or just omit service labels entirely. The selector (lines 13-15) is what matters, and that's perfect!

⚠️ frontend-deployment.yaml

This shows you forgot --dry-run=client! You can see all the extra fields:
•  Line 10: progressDeadlineSeconds: 600
•  Line 12: revisionHistoryLimit: 10
•  Lines 33-39: All the default fields

These aren't wrong, but they're unnecessary noise. Compare to your clean backend-deployment.yaml!

⚠️ frontend-svc-nodeport.yaml

Lines 19-20:
yaml
Same issue - remove status field.

Line 10:
yaml
Minor: The port name usually describes the protocol (e.g., name: http or name: 80-80). Not wrong, just unconventional.

Additional Commands You Should Know

List all services with details:
bash
Quick service verification:
bash
Key Takeaways for CKAD:
•  Always verify your work with functional tests (curl, wget, etc.)
•  Services use selectors to find pods - labels must match!
•  ClusterIP = internal only, NodePort = external access
•  --dry-run=client creates clean YAML every time
•  Remove status: fields from YAML before applying
•  Test pod-to-service connectivity with kubectl exec ... -- curl

Score: 90/100 - Excellent understanding! Just remember --dry-run=client consistently and clean up status fields.

Ready for question 6?
~/workspace/study/ckad-prep/warp-session1
Not just yet, I am trying to fix the NodePort cause I am not able to see the nginx page from the node port curl
Good practice to troubleshoot! Let me help you debug the NodePort issue. Let's check step by step:
Pod is running. Let's check the endpoints:
Perfect! Endpoint exists. Now let's check what Kubernetes cluster you're using:
You're using Docker Desktop Kubernetes! This is the issue. Let me get the node info:
The Problem: Docker Desktop Kubernetes

With Docker Desktop Kubernetes, NodePort services work differently. The node IP 192.168.65.3 is internal to Docker Desktop's VM.

Solutions to Access NodePort on Docker Desktop:

Option 1: Use localhost (EASIEST)
bash
Docker Desktop automatically forwards NodePort services to localhost!

Option 2: Use the ClusterIP from within the cluster
bash
Option 3: Port-forward (alternative)
bash
Try the first option:
✅ It Works!

Your NodePort is working perfectly! Use:
bash
Why This Happens:

| Cluster Type | NodePort Access Method |
|--------------|------------------------|
| Docker Desktop | localhost:30080 |
| Minikube | minikube ip:30080 or minikube service |
| Kind | Need port mapping or localhost:30080 |
| Cloud (GKE/EKS/AKS) | <node-external-ip>:30080 |
| Real cluster | Any node IP:30080 |

For CKAD Exam:
The exam environment typically uses standard clusters where you can access NodePort via the node's IP. But knowing these variations is helpful for practice!

Your configuration is correct - it was just an access method issue. Ready for question 6?