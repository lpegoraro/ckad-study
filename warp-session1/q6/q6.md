Question 6: Init Containers and Persistent Volumes

Create a Pod with an init container that prepares data for the main application.

Requirements:

1. Create a PersistentVolumeClaim named app-pvc with:
-  Storage class: Use the default storage class
-  Access mode: ReadWriteOnce
-  Storage request: 100Mi
2. Create a Pod named data-app with:
-  Init Container (name: data-init):
-  Image: busybox:1.35
-  Command: Create a file /data/config.txt with content "initialized at $(date)"
-  Mount the PVC at /data
-  Main Container (name: app):
-  Image: nginx:alpine
-  Mount the PVC at /usr/share/nginx/html
-  Should only start after init container completes
3. The init container must complete successfully before the main container starts

Please provide:
1. YAML or command to create the PersistentVolumeClaim
2. YAML for the Pod with both init and main containers
3. Command to verify the init container ran successfully
4. Command to verify the file was created in the shared volume
5. Command to check the pod events to see the init container execution


CKAD Tip: Init containers run sequentially before the main containers. They're commonly used for setup tasks, waiting for dependencies, or preparing data. This is a frequent exam topic!

Go ahead and attempt this question!

To separate things a bit, I will start using a ckad-exam1-q6 namespace for if I want to get back or separate contexts for this.

I could not find anywhere, how should I create pvc from a command, I tried even getting a default but with no luck, if there is any please let me know, I created the YAML from the template and I used the storage class for hostpath (which is the default here)

I then applied the q6/app-pvc.yaml into k8s, then I created the basic YAML using the `k run data-app --image=busybox:1.35 --dry-run=client -o yaml > data-app-pod.yaml`
then I added the initContainer (which must be AFTER the container, learned that the hard way), and then I applied the q6/data-app-pod.yaml.

To verify the initContainer I used this command `k get pod data-app -o jsonpath='{range .status.initContainerStatuses[*]}{.name}{": "}{.state.waiting.reason}{.state.running.reason}{.state.terminated.reason}{end}{"\n"}'`

To verify the container stateu I used this command
`k get pod data-app -o jsonpath="{.spec['containers'][*].name}{': '}{.status.containerStatuses[*].state}{'\n'}"`


Great work! Let me address your questions and evaluate your solution.

Creating PVC Imperatively

Unfortunately, there is NO kubectl imperative command to create PVCs directly. You must use YAML. This is one of the resources where you need to know the YAML structure or use a template.

Quick PVC YAML template to memorize for CKAD:
yaml
Pro tip: Create a snippet file or remember this minimal structure for the exam!

Init Container Order - Important Clarification!

You said: "initContainer must be AFTER the container, learned that the hard way"

❌ This is INCORRECT! In the YAML structure, initContainers should come BEFORE containers:
yaml
If you put it after and it worked, Kubernetes might have accepted it, but the standard convention and best practice is initContainers first.

Now let me check your files:
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/q6/app-pvc.yaml
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/q6/data-app-pod.yaml
YAML Review

✅ app-pvc.yaml - PERFECT!
Clean, correct PVC definition. Well done!

⚠️ data-app-pod.yaml - Issues Found

1. Lines 8-13 vs 14-20 - Init Container Order
yaml
While Kubernetes accepts this, best practice is initContainers first. Reorder them:
yaml
2. Lines 12 & 19-20 - Mount Path Mismatch

This is a critical logic error:
•  Init container writes to /data/config.txt
•  Main container mounts volume at /usr/share/nginx/html
•  Result: nginx serves /usr/share/nginx/html/config.txt (correct!)

But init container writes to a different path! Both should mount at the same base path or you need to adjust paths:

Option A (Simpler): Mount both at /data, init writes to /data/config.txt:
yaml
Option B (Your intent): Init container should write to /usr/share/nginx/html/config.txt:
yaml
Your Verification Commands

Init container status command:

k get pod data-app -o jsonpath='{range .status.initContainerStatuses[*]}{.name}{": "}{.state.waiting.reason}{.state.running.reason}{.state.terminated.reason}{end}{"\n"}'

✅ This is advanced! But overly complex for the exam. Simpler alternatives:

# Simple and effective
k describe pod data-app | grep -A 10 "Init Containers"

# Or just check status
k get pod data-app

# Or see events
k get events --sort-by='.lastTimestamp' | grep data-app

File verification - Best approach:

# Check if file exists and show content
k exec data-app -- cat /usr/share/nginx/html/config.txt

# Or if using /data mount
k exec data-app -- cat /data/config.txt