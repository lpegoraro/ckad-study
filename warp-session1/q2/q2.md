Question 2: Multi-Container Pod with Shared Volume

Create a Pod named log-processor in the default namespace with:
•  Container 1 (name: writer):
◦  Image: busybox:1.35
◦  Command: continuously write the current date/time to /var/log/app.log every 5 seconds
◦  Should mount a volume at /var/log
•  Container 2 (name: reader):
◦  Image: busybox:1.35
◦  Command: continuously tail and display /var/log/app.log
◦  Should mount the same volume at /var/log
•  Volume:
◦  Type: emptyDir
◦  Both containers should share this volume

Please provide:
1. The YAML manifest for this Pod
2. The kubectl command to apply it
3. A command to verify both containers are running
4. A command to check the logs from the reader container to confirm it's seeing the writer's output



CKAD Tip: This tests multi-container pods, shared volumes, and command specifications - common exam topics!

Go ahead and attempt this question!

Evaluation

✅ Correct:
•  k apply -f log-processor.yaml is correct
•  k logs log-processor -c reader is the right way to get container-specific logs
•  Adding -f flag for following/tailing logs shows good understanding
•  Using custom-columns shows advanced kubectl knowledge!

📝 Feedback on verification command:

Your custom-columns approach is creative, but there's a simpler way that's faster in the exam:
bash
Now let me check your YAML:
/home/lpegoraro/workspace/study/ckad-prep/warp-session1/log-processor.yaml
YAML Review

⚠️ Issue found (line 12):

The watch command approach has a problem:
yaml
Problem: watch runs date but doesn't redirect its output properly - the >> is inside the watched command context, which won't work as expected.

✅ Better approach:
yaml
Alternative (also correct):
yaml
✅ Everything else is perfect:
•  emptyDir volume configured correctly
•  Volume mounts on both containers are correct
•  Container names and images are correct
•  The reader container's tail command is correct

Key Takeaways for CKAD:
•  Use while loops with sleep for periodic tasks in containers - more reliable than watch
•  Multi-container pods share the same network namespace and can share volumes
•  Always specify -c <container-name> when getting logs from multi-container pods
•  The >> operator appends to files (vs > which overwrites)

Score: 90/100 - Great understanding! Just needed the command fix.